# Production: FastAPI backend URL (no trailing slash). Vercel proxies /api/* to this host.
NEXT_PUBLIC_API_URL=https://your-api.onrender.com

# Set to "false" to connect to live FastAPI backend instead of mock data
NEXT_PUBLIC_USE_MOCK=false
